<view class="page">
  <text class="title">🕒 模拟测试</text>

  <!-- 加载状态 -->
  <view wx:if="{{loading}}">加载中...</view>

  <!-- 空题占位 -->
  <view wx:elif="{{!loading && !questions.length && !isFinished}}">
    暂无测试题，请稍后
  </view>

  <!-- 测试题显示 -->
  <block wx:if="{{!loading && !isFinished && questions.length}}">
    <text class="timer">剩余时间：{{timeLeft}} 秒</text>
    <text class="question">{{questions[currentIndex].question}}</text>

    <view class="options">
      <block wx:for="{{questions[currentIndex].options}}" wx:key="*this">
        <button bindtap="selectOption" data-option="{{item}}">
          {{item}}
        </button>
      </block>
    </view>
  </block>

  <!-- 测试完成 -->
  <view wx:if="{{isFinished}}">
    <text class="result">🎯 测试完成！得分：{{score}} / {{questions.length}}</text>
  </view>
</view>
